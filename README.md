README — Existing Problem Fix Updates

Purpose

This update contains code written to fix existing problems in the project. The files are organized according to where they need to be applied.

File Replacement & Update Instructions

The changes should be applied as follows:

1. Backend/

The files provided inside the Backend folder contain fixes for existing backend problems.

Action:
Replace the corresponding existing files in the project's Backend directory with the files from this update.

> Do not keep the old versions of the files if the updated versions have the same filename and purpose.



2. Data/cases/

The files inside Data/cases contain corrected/updated case data required by the fixes.

Action:
Replace the corresponding existing files inside Data/cases/ with the files provided in this update.

3. Update/

The files inside the Update folder are not necessarily replacements. They contain modifications that need to be applied to the existing project files.

Action:
Update the corresponding existing files according to the contents of the files in Update/.

Important

Backend/ → Replace existing corresponding files

Data/cases/ → Replace existing corresponding files

Update/ → Update/merge the existing files

Do not delete unrelated existing files.

Do not modify frontend/UI files unless explicitly included in the update.

Preserve the existing project structure and functionality.

The purpose of these changes is to fix existing problems without breaking currently working features.
